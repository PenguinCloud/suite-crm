---
- name: Create empty repo for suite crm
  file:
    path: /opt/suitecrm
    state: directory

- name: Download and extract SuiteCRM
  ansible.builtin.unarchive:
    src: "{{ app_link }}"
    dest: /opt/suitecrm
    remote_src: yes